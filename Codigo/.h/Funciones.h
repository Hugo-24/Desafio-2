#ifndef FUNCIONES_H
#define FUNCIONES_H

// Copia una cadena de texto a memoria nueva
char* copiarTexto(const char* fuente);

// Compara si dos cadenas son exactamente iguales
bool sonIguales(const char* a, const char* b);

// Verifica si una cadena comienza con un prefijo
bool iniciaCon(const char* texto, const char* prefijo);

#endif
